# Configuration

Use `settings.example.yaml` as a non-secret reference. Runtime credentials are loaded from environment variables or the existing local Web3 `.env`; they are never copied into this project.
