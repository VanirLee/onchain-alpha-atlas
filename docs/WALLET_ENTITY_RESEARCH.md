# Wallet/entity research

The wallet skill engine estimates a time-varying, shrinkage-adjusted skill
history only from trades closed before each measurement time. It supports
minimum trade counts and does not assign a permanent smart-money label.

The current verified dataset has no wallet trade/holdings panel, so H003 is
`BLOCKED_BY_DATA`. No synthetic wallet result is generated.
